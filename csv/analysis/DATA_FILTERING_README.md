# 數據集分析與過濾工具

## 概述

本工具用於分析 Codec 評估數據集的指標分布，過濾異常值，並生成乾淨的訓練集和測試集。

## 負 DCER/DWER 問題分析報告

### 問題根源

在分析評估結果時發現部分 codec 出現負的 DCER/DWER 值，經過詳細調查後發現**這不是 ASR 模型的問題**，而是**原始音頻本身存在問題**。

#### 主要問題類型

1. **原始音頻被污染**（主要問題）
   - 部分原始音頻包含 YouTube 訂閱提示語等非目標內容
   - 這些干擾內容被重複多次，導致原始音頻的 WER/CER 非常高
   - Codec 重建後的音頻反而"修正"了這個問題，使得 inference 音頻的錯誤率更低
   - 結果：DWER/DCER = inference_error - original_error < 0

   **案例：VIVOSDEV07_246**
   - Ground Truth: `CHÍN MƯƠI TÁM CHÍN MƯƠI CHÍN` (98 99)
   - 原始音頻 WER: 39.67 (包含大量重複的訂閱提示)
   - Inference 音頻 WER: 2.5
   - DWER: -37.17

2. **ASR 幻覺（Hallucination）**
   - ASR 模型對某些原始音頻產生嚴重幻覺
   - 轉寫出與 Ground Truth 完全不相關的內容

3. **MDCC (粵語) 特殊問題**
   - Whisper 使用普通話模式轉寫粵語內容，存在語言混淆
   - 部分原始音頻有背景干擾

### 建議

1. **使用中位數（Median）而非平均值（Mean）**
   - 中位數對極端異常值不敏感，更能反映真實性能
   - 大多數 codec 的 DCER/DWER Median 都接近 0，這是更可靠的指標

2. **過濾異常樣本**
   - 建議過濾掉 DCER/DWER < -2 或 > 5 的極端樣本
   - 使用本工具自動過濾這些異常數據

3. **數據清洗**
   - 建議檢查並移除原始音頻有問題的樣本
   - 使用過濾後的乾淨數據集進行評估

## 生成的數據集

### Vietnamese (Vivos)

**路徑**: `/home/jieshiang/Desktop/GitHub/Codec_comparison/csv/analysis/vivois/`

- **訓練集**: `vivos_filtered_train.csv` (4,355 樣本)
- **測試集**: `vivos_filtered_test.csv` (1,000 樣本)

**過濾標準**:
- Duration: 1.0 ~ 15.0 秒
- WER: 0.0 ~ 1.5
- MOS Quality: 2.0 ~ 5.0
- MOS Naturalness: 1.5 ~ 4.5
- TER: 0.0 ~ 0.5

**測試集統計** (最乾淨的 1000 筆):
- WER: 平均 0.99, 中位數 1.0
- MOS Quality: 平均 3.98, 中位數 4.01
- MOS Naturalness: 平均 3.23, 中位數 3.24
- TER: 平均 0.03, 中位數 0.0

### Cantonese (MDCC)

**路徑**: `/home/jieshiang/Desktop/GitHub/Codec_comparison/csv/analysis/mdcc/`

- **訓練集**: `mdcc_filtered_train.csv` (4,582 樣本)
- **測試集**: `mdcc_filtered_test.csv` (1,000 樣本)

**過濾標準**:
- Duration: 1.0 ~ 15.0 秒
- CER: 0.0 ~ 1.5
- MOS Quality: 2.0 ~ 5.0
- MOS Naturalness: 1.5 ~ 4.5
- TER: 0.0 ~ 0.5

**測試集統計** (最乾淨的 1000 筆):
- CER: 平均 0.085, 中位數 0.103
- MOS Quality: 平均 4.11, 中位數 4.22
- MOS Naturalness: 平均 3.52, 中位數 3.66
- TER: 平均 0.052, 中位數 0.0

## 使用方法

### 1. 快速使用（推薦）

已為你準備好便捷腳本：

```bash
# 過濾越南語數據集
cd /home/jieshiang/Desktop/GitHub/Codec_comparison/csv/analysis/vivois
bash filter_vivos_dataset.sh

# 過濾粵語數據集
cd /home/jieshiang/Desktop/GitHub/Codec_comparison/csv/analysis/mdcc
bash filter_mdcc_dataset.sh
```

### 2. 手動使用（自定義參數）

```bash
# 越南語數據集
python3 analyze_and_filter_dataset.py \
    --csv vivois/vivos_filtered_full.csv \
    --language vi \
    --output_dir vivois/ \
    --test_size 1000 \
    --existing_test vivois/vivos_filtered_test_clean.csv \
    --filter_method threshold \
    --plot

# 粵語數據集
python3 analyze_and_filter_dataset.py \
    --csv mdcc/mdcc_filtered_full.csv \
    --language yue \
    --output_dir mdcc/ \
    --test_size 1000 \
    --filter_method threshold \
    --plot
```

### 3. 參數說明

- `--csv`: 輸入 CSV 文件路徑
- `--language`: 語言類型 (`vi` 越南語, `yue` 粵語)
- `--output_dir`: 輸出目錄
- `--test_size`: 測試集大小（默認 1000）
- `--existing_test`: 現有測試集 CSV（用於排除，避免重複）
- `--filter_method`: 過濾方法
  - `threshold`: 使用預定義閾值（推薦）
  - `iqr`: 使用 IQR 統計方法
  - `percentile`: 使用百分位數
- `--plot`: 生成分布圖

## 輸出文件

每次執行後會生成：

1. **{dataset}_filtered_train.csv** - 訓練集
2. **{dataset}_filtered_test.csv** - 測試集（按錯誤率排序，選最乾淨的）
3. **{dataset}_filter_summary.txt** - 詳細統計摘要
4. **{dataset}_distributions.png** - 指標分布可視化圖表

## 過濾策略

### 測試集選擇策略

- 按錯誤率（WER/CER）升序排序
- 選擇錯誤率最低的前 1000 筆作為測試集
- 排除已存在於舊測試集中的樣本（避免重複）

### 訓練集策略

- 剩餘所有符合閾值的樣本
- 保證與測試集無重疊

## 數據質量

過濾後的數據集保證：

1. ✅ 所有樣本的 WER/CER <= 1.5
2. ✅ MOS Quality 分數合理（2.0 ~ 5.0）
3. ✅ TER（聲調錯誤率）<= 0.5
4. ✅ 音頻時長在合理範圍（1 ~ 15 秒）
5. ✅ 測試集選擇錯誤率最低的樣本（最乾淨）
6. ✅ 訓練集與測試集無重疊

## 工具特性

- 🔍 **自動分析**: 分析所有指標的分布統計
- 📊 **可視化**: 生成分布圖和箱型圖
- 🎯 **智能過濾**: 三種過濾方法可選
- 📝 **詳細報告**: 生成完整的統計摘要
- 🔄 **去重機制**: 避免測試集與訓練集重疊
- ⚡ **高效處理**: 使用 pandas 快速處理大規模數據

## 依賴安裝

```bash
pip install pandas numpy matplotlib seaborn
```

## 問題與建議

如有問題或建議，請聯繫數據集維護者。

---

**最後更新**: 2026-01-16
**數據集版本**: v1.0
**工具版本**: 1.0.0
